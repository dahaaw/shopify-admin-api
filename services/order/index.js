const getByName = require( './getByName' );
const setArchived = require('./setArchived');
const setCancelled = require( './setCancelled' );
const fulfilled = require( './fulfilled' );
const setUnarchived = require( './setUnarchived' );
module.exports = {
    getByName,
    setArchived,
    setCancelled,
    fulfilled,
    setUnarchived
}